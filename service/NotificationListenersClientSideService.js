'use strict';


/**
 * Client listener for entity CatalogBatchEvent
 * Example of a client listener for receiving the notification CatalogBatchEvent
 *
 * body CatalogBatchEvent The event data
 * returns EventSubscription
 **/
exports.listenToCatalogBatchEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity CatalogCreateEvent
 * Example of a client listener for receiving the notification CatalogCreateEvent
 *
 * body CatalogCreateEvent The event data
 * returns EventSubscription
 **/
exports.listenToCatalogCreateEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity CatalogDeleteEvent
 * Example of a client listener for receiving the notification CatalogDeleteEvent
 *
 * body CatalogDeleteEvent The event data
 * returns EventSubscription
 **/
exports.listenToCatalogDeleteEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity CategoryCreateEvent
 * Example of a client listener for receiving the notification CategoryCreateEvent
 *
 * body CategoryCreateEvent The event data
 * returns EventSubscription
 **/
exports.listenToCategoryCreateEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity CategoryDeleteEvent
 * Example of a client listener for receiving the notification CategoryDeleteEvent
 *
 * body CategoryDeleteEvent The event data
 * returns EventSubscription
 **/
exports.listenToCategoryDeleteEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingAttributeValueChangeEvent
 * Example of a client listener for receiving the notification ProductOfferingAttributeValueChangeEvent
 *
 * body ProductOfferingAttributeValueChangeEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingAttributeValueChangeEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingCreateEvent
 * Example of a client listener for receiving the notification ProductOfferingCreateEvent
 *
 * body ProductOfferingCreateEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingCreateEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingDeleteEvent
 * Example of a client listener for receiving the notification ProductOfferingDeleteEvent
 *
 * body ProductOfferingDeleteEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingDeleteEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingPriceAttributeValueChangeEvent
 * Example of a client listener for receiving the notification ProductOfferingPriceAttributeValueChangeEvent
 *
 * body ProductOfferingPriceAttributeValueChangeEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingPriceAttributeValueChangeEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingPriceCreateEvent
 * Example of a client listener for receiving the notification ProductOfferingPriceCreateEvent
 *
 * body ProductOfferingPriceCreateEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingPriceCreateEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingPriceDeleteEvent
 * Example of a client listener for receiving the notification ProductOfferingPriceDeleteEvent
 *
 * body ProductOfferingPriceDeleteEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingPriceDeleteEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingPriceStateChangeEvent
 * Example of a client listener for receiving the notification ProductOfferingPriceStateChangeEvent
 *
 * body ProductOfferingPriceStateChangeEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingPriceStateChangeEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductOfferingStateChangeEvent
 * Example of a client listener for receiving the notification ProductOfferingStateChangeEvent
 *
 * body ProductOfferingStateChangeEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductOfferingStateChangeEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductSpecificationCreateEvent
 * Example of a client listener for receiving the notification ProductSpecificationCreateEvent
 *
 * body ProductSpecificationCreateEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductSpecificationCreateEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}


/**
 * Client listener for entity ProductSpecificationDeleteEvent
 * Example of a client listener for receiving the notification ProductSpecificationDeleteEvent
 *
 * body ProductSpecificationDeleteEvent The event data
 * returns EventSubscription
 **/
exports.listenToProductSpecificationDeleteEvent = function(body) {
  return new Promise(function(resolve, reject) {
    var examples = {};
    examples['application/json'] = {
  "query" : "query",
  "callback" : "callback",
  "id" : "id"
};
    if (Object.keys(examples).length > 0) {
      resolve(examples[Object.keys(examples)[0]]);
    } else {
      resolve();
    }
  });
}

